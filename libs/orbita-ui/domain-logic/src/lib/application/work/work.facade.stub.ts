import { WorkFacadeAbstract } from './work.facade.abstract';
import { Work } from '../../entities/models/work.interface';

export class WorkFacadeStub implements WorkFacadeAbstract {
  replaceAllWorks(works: Work[]) {
    /** */
  }

  setWorks(works: Work[]) {
    /** */
  }
}
